{
  "name": "vault-desktop",
  "version": "1.0.0",
  "description": "Vault Finance — Desktop app (Windows, Mac, Linux) via Tauri",
  "scripts": {
    "dev": "tauri dev",
    "build": "tauri build",
    "tauri": "tauri"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.0.0"
  },
  "dependencies": {
    "@tauri-apps/api": "^2.0.0"
  }
}
